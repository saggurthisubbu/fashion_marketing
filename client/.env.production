# Production Environment Configuration
# If backend is hosted on Vercel Serverless (same domain): Leave empty or set to /api
# If backend is hosted separately on Render / Railway: Set to https://your-backend.onrender.com/api
VITE_API_URL=
