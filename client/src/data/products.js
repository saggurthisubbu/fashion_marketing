/**
 * QuickFit Products Data
 * Single Source of Truth: Cloud MongoDB Database via /api/products
 * Static dummy data removed. All products are dynamically retrieved from MongoDB Atlas.
 */

export const productsData = [];
export default productsData;
